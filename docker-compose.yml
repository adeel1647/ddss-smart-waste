version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ddss
    ports:
      - "5432:5432"
    volumes:
      - ddss_pg_data:/var/lib/postgresql/data
volumes:
  ddss_pg_data:
